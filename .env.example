# Exemplo de configuração (.env) – copie para .env e ajuste valores reais
# Banco de Dados (usar apenas um bloco; se usar SQLite simples deixe só DB_ENGINE=sqlite)
DB_ENGINE=sqlite
DB_HOST=localhost
DB_PORT=3306
DB_USER=usuario
DB_PASSWORD=senha
DB_NAME=hidrometro
# Opcional: URL completa (sobrepõe os campos acima se definida)
# DATABASE_URL=mysql+pymysql://usuario:senha@localhost:3306/hidrometro

# Limite de histórico em memória (deque)
HISTORY_LIMIT=1000

# MQTT
MQTT_URL=mqtt://broker.hivemq.com:1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_TOPIC_DADOS=hidrometro/dados
MQTT_TOPIC_CMD=hidrometro/cmd

# JWT / Segurança
SECRET_KEY=changeme-dev
JWT_EXP_MINUTES=60

# Outras flags opcionais
# DEBUG_HISTORY=1
# LOG_LEVEL=INFO

# Para produção gere uma SECRET_KEY forte (exemplo Python):
# python -c "import secrets; print(secrets.token_hex(32))"
